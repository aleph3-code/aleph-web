import Section from '../layout/Section';

export default function Contact() {
    return (
        <Section id="contact">
            <h1 className="text-4xl font-bold">Contact</h1>
            <p className="mt-4 text-xl">
                ℵ Get in touch with us to learn more about our services.
            </p>
        </Section>
    );
}
