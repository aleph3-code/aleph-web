import Section from '../layout/Section';

export default function Tools() {
    return (
        <Section id="tools">
            <h1 className="text-4xl font-bold">Tools</h1>
            <p className="mt-4 text-xl">
                ℵ We use the latest tools and technologies to deliver the best
                results.
            </p>
        </Section>
    );
}
